import React from "react";
import { Container } from "react-bootstrap";

function Header() {
  return (
    <Container className="mt-4 mb-4 header-container">
      <h1>Convert HEIC files to PNG files</h1>
    </Container>
  );
}

export default Header;
